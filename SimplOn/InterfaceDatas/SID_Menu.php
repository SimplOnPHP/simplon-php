<?php

class SID_Menu extends SID_Container{

    // function __construct($sources) {
	// 	$this->menuLinks = new SID_Container($sources);
	// 	if(SC_Main::$SystemMessage){
	// 		$this->systemMessage = new SID_SystemMessage(SC_Main::$SystemMessage);
	// 	}else{
	// 		$this->systemMessage = new SD_emptyAction('',array());
    //     }
    // }
}